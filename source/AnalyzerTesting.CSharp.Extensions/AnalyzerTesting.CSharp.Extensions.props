<Project>
  <ItemGroup Condition="$(Language) == 'C#' and ($(ImplicitUsings) == 'enable' or $(ImplicitUsings) == 'true')">
    <Using Include="AnalyzerTesting.CSharp.Extensions.Transforms" Static="True" />
  </ItemGroup>
</Project>